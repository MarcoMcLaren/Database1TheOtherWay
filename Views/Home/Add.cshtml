
@{
    ViewBag.Title = "Add";
}

<h2>Add</h2>
@using (Html.BeginForm("Add", "Home", FormMethod.Post))
{
    <label>Name</label>
    <br />
    @Html.TextBox("name", null, new { @class = "form-control", @type = "text" });
    <label>Website</label>
    <br />
    @Html.TextBox("website", null, new { @class = "form-control", @type = "text" });
    <button type="submit">Save</button>
}
